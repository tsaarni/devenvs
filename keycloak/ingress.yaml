apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: keycloak
spec:
  tls:
  - hosts:
      - keycloak.127-0-0-121.nip.io
    secretName: keycloak-external
  defaultBackend:
    service:
      name: keycloak
      port:
        number: 8080
